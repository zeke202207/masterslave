namespace NetX.Master
{
    public enum JobStatus
    {
        Pending,
        Running,
        Completed,
        Failed
    }

    public enum WorkNodeStatus
    {
        Idle,
        Busy
    }
}
